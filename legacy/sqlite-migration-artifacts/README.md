# Legacy SQLite Migration Artifacts

These files are retained only for historical reference from the SQLite -> Postgres cutover.

- They are **not used by runtime services**.
- Active runtime is Postgres-only.
- Keep for audit trail / rollback history only.
